{
	"private": true,
	"type": "module",
	"scripts": {
        "postinstall": "./apply-tiptap-patch.sh && cd frontend && yarn install",
		"dev": "cd frontend && yarn dev",
		"build": "cd frontend && yarn build",
		"upgrade-frappeui": "cd frontend && yarn add frappe-ui@latest && cd .."
	}
}
