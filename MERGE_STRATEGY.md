# Практическая стратегия слияния с frappe/main

## 🎯 Быстрый старт

```bash
# 1. Подготовка (создать backup)
./merge-prepare.sh

# 2. Выполнить мерж
git checkout -b merge-frappe-main develop
git merge frappe/main --no-commit --no-ff

# 3. Восстановить наши файлы
./merge-restore.sh category_a

# 4. Вручную объединить категорию B (см. ниже)

# 5. Проверить и закоммитить
./merge-verify.sh
git commit -m "merge: merge frappe/main with custom features"

# 6. Сохранить переводы (КРИТИЧНО!)
git checkout --ours crm/translations/ru.csv  # Ваши переводы более полные!
git checkout --ours crm/locale/ru.po
./find-hardcoded-strings.sh  # Найти захардкоженные строки

# 7. Доработки (исправить несогласованности)
# - Исправить онбоардинг в InviteUserPage.vue и ChangePasswordModal.vue
# - Исправить захардкоженные строки в новых файлах
# - Добавить переводы для новых строк из frappe/main
# - Проверить другие файлы
# - Обновить документацию
# См. ЭТАП 6 в MERGE_ANALYSIS_REPORT.md
```

---

## 📂 Категории файлов

### 🟢 Категория A: Полностью наши (восстановить из backup)
- Avito интеграция
- permissions.py
- API файлы (performance, system, communication, etc.)
- DocTypes (CRM Funnel, Sales Funnel reports)
- Frontend компоненты (SingleImageUploader, onboarding)

### 🟡 Категория B: Нужно объединить (ручное слияние)
- hooks.py (наши hooks + их новые schedulers)
- crm/api/doc.py (наша on_doc_update + их изменения)
- frontend/src/App.vue (наш Redis warmup + их NotPermitted)

### 🔵 Категория C: Принять их + проверить наши места
- crm/www/crm.py
- DocTypes JSON файлы (проверить поле sales_funnel)

### 🔴 Категория D: Переводы (КРИТИЧНО - ваши более полные!)
- ru.csv (457 строк - в frappe/main меньше!)
- ru.po (более полные переводы)
- Кастомные файлы переводов статусов
- **Действие:** Сохранить ваши версии + добавить переводы для новых строк

### 🟢 Категория E: Безопасно принять все изменения
- Локализации других языков (кроме ru)
- Новые функции (Facebook Lead Forms, etc.)
- Зависимости (package.json, yarn.lock)

---

## 📝 Детальный план

См. `MERGE_ANALYSIS_REPORT.md` раздел "ПРАКТИЧЕСКАЯ СТРАТЕГИЯ СЛИЯНИЯ"

